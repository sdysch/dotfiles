# themes
colorscheme monokai
#colorscheme tomorrow

# change to next/previous tracks
bind -f library < player-prev 
bind -f library > player-next

set status_display_program=~/.config/cmus/status_display_program.sh
