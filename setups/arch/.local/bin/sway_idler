#!/bin/sh

swayidle -w \
	timeout 570 'notify-send -i dialog-warning "Screen locking in 30s"' \
	timeout 600 'lock_sway' \
	timeout 600 'swaymsg "output * dpms off"' \
	resume 'swaymsg "output * dpms on"' \
	before-sleep 'lock_sway'

