#!/bin/bash

shopt -s nullglob

for f in "$@"; do
    echo "File: $f"
    ffprobe -v error \
      -show_entries format_tags=title,album,artist \
      -of default=noprint_wrappers=1:nokey=0 "$f"
    echo
done

