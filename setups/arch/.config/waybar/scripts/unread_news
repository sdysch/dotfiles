#!/bin/bash

# Fetch unread items once
unread=$(newsboat -x print-unread | awk '{ print $1 }')

# Output JSON
if [ "$unread" -gt 0 ]; then
    printf '{"text":"📰 %s","tooltip":"%s"}\n' "$unread" "$unread Unread articles"
else
    printf '{"text":"📰 0","tooltip":"No unread articles"}\n'
fi
